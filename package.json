{
  "name": "@voltforecast/template-app",
  "type": "module",
  "engines": {
    "node": "20.x",
    "pnpm": ">=6.0.0",
    "npm": "please use pnpm"
  },
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "typecheck": "pnpm -r --if-present run typecheck",
    "check": "pnpm -r --if-present run check",
    "fix": "pnpm -r --if-present run fix"
  },
  "devDependencies": {
    "sharp": "^0.34.4"
  }
}
